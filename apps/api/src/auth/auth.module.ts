import { Module } from '@nestjs/common';

/**
 * Auth module
 * Better Auth handles authentication via its own routes.
 * This module can be extended with additional auth-related controllers.
 */
@Module({
  controllers: [],
})
export class AuthControllersModule {}
